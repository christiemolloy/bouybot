import * as React from 'react';
import { PageSection, Title } from '@patternfly/react-core';

const Temperature: React.FunctionComponent<{}> = () => (
    <PageSection>
      <Title size="lg">Temperature: 17c</Title>
    </PageSection>
  )

export { Temperature };
